/**
 * Minimum viewport width the challenge workspace (two/three-pane Monaco
 * editors, mouse-only resizable gutters) can actually lay out in.
 * Mirrors `--breakpoint-workspace` in app/global.css — keep both in sync.
 */
export const WORKSPACE_MIN_WIDTH = 900
